A set of values and the operations that can be carried out with those values are called _______.literals numbers values typestypesWhat is the name of the type that denotes floating-point numbers that can have fractional parts? doublefloatingPoint int integer doubleWhat is the name of the type that denotes whole numbers? double intwhole integer intWhich is a valid number literal? 1x10^4 1E4 3 1/2 10,000 1E4 What is the name of the type that denotes a string of characters? Characters char charString String StringWhich of the following values does not have a primitive type?12.50 ''Hello''6 1E4 ''Hello''What is a storage location in the computer's memory called that has a type, name, and contents?identifier literal label variable variable Which of the following declares a variable that will store a welcome message?String welcome; double welcome; Char welcome; int welcome; String welcome; Which of the following declares a variable that will store a measurement with fractional parts?int measure; double measure; String measure; integer measure; double measure; Which of the following declares a variable that will store a count with an integer value?integer count; double count; String count; int count; int count; Which term is used to describe the name of a variable, method, or class?type literal identifier label identifier By convention, variables begin with a(n) _____________.uppercase letter digit lowercase letter dollar sign lowercase letter By convention, classes begin with a(n) _____________.lowercase letter dollar sign digit uppercase letter uppercase letter Which of the following is the best choice for a variable identifier that will store a name?name Name n nm name What is the name of the = operator in Java?inequality assignment identity equality assignment What is the purpose of the assignment operator?to check for inequality to check for identity to check for equality to change the value of a variable to change the value of a variable Which statement declares a variable that will store an integer value?integer count; int count; count = 5; String count = 5; int count; Which statement stores an integer value in a variable?count = 5; String count = 5; int count; integer count = 5; count = 5; Which statement declares and stores an integer value in a variable?count = 5; int count = 5; integer count = 5; String count = 5; int count = 5; Assume that the variable count has been declared as type int, which statement changes the value of count?count = 6; count == 6; integer count = 6; count := 6; count = 6; Assume that the variable count has been declared as type int. Which statement adds 10 to count?count = 10; count == count + 10; count = count + 10; count + 10; count = count + 10; What is an object?A sequence of instructions. Any value stored in a variable. An entity in your program that is manipulated by calling methods. Any input to a method. An entity in your program that is manipulated by calling methods. What is the type of an object?variable method reference class class Which of the following statements about objects is correct?An object defines the methods for a class. Every object belongs to a class.An object is a sequence of instructions. All entities, even numbers, are objects. Every object belongs to a class.Which is not a method of the String class?length toUpperCase toLowerCase println println In Java, every value is either a reference to an object, or it belongs to one of the eight _________.primitive typesnumber types char types boolean types primitive typesIn Java, which of the following is not an integer primitive type?byte long double short double In Java, there are four integer primitive types with different ranges of integer values. Which ordering of the integer types lists the types in increasing order by range?byte, int, short, long long, int, short, byte byte, short, long, int byte, short, int, long byte, short, int, long A numeric computation ___________ if the result falls outside the range for the number type.overflows concatenates truncates rounds overflows What type carries out floating-point computation without roundoff errors?BigInteger BigDecimal float double BigDecimal The decimal equivalent of 110100 binary is ____.26 48 52 70 52 The decimal equivalent of 111010 binary is ____.142 72 58 112 58 Numerical ____________________ are values that do not change and that have a special significance for a computation.constants statics comments commands constants In Java, what reserved word is used to identify constants?final double constant int final Which of the following correctly defines a constant in a method?public static final double L_P_G==3.785 final double NICKEL_VALUE == 0.05; final double NICKEL_VALUE = 0.05; public static final double L_P_G=3.785; final double NICKEL_VALUE = 0.05; Which of the following correctly defines a constant in a method?final int HOURS_PER_DAY = 24; final int DAYS_PER_YEAR == 365; public static final int D_P_Y=365; public static final int H_P_D==24; final int HOURS_PER_DAY = 24; Which of the following correctly defines a constant in a class?public static final int H_P_D==24; final int HOURS_PER_DAY = 24; final int DAYS_PER_YEAR == 365; public static final int D_P_Y=365;public static final int D_P_Y=365; What are the arithmetic operators (add, subtract, multiply, divide) in Java, respectively?+, -, x, / +, -, x, % +, -, *, / add, subtract, multiply, divide +, -, *, / What is the name of the operation denoted by the ++ operator?decrement addition increment plus increment What is the name of the operation denoted by the -- operator?subtraction minus increment decrement decrement What is the value of x after the following sequence of statements?  x--; x++;x - 1 x + 1 x 0 x What is the result of a / b when variables a and b are declared as follows: int a = 9; double b = 5;?.80 4 1 1.80 1.80 What is the Java operator that computes the remainder of an integer division?% mod / div % Which statement correctly computes the square root of the given number?int s = Math.sqrt(16); int s = Math.squareroot(16); double s = Math.sqrt(16); double s = Math.squareroot(16); double s = Math.sqrt(16); Which statement correctly computes a specified number raised to the specified power?double p = 2.pow(3); double p = Math.pow(2,3); double p = 2.power(3); double p = Math.power(2,3); double p = Math.pow(2,3); Which statement correctly computes the absolute value of the difference between x and y?Math.abs(x-y) Math.absoluteValue(x-y) Math.absolute(x-y) |x-y| Math.abs(x-y) What is the term that means to explicitly convert a value to a different type?case overflow rounding cast cast Which of the following code fragments converts a floating-point number to the nearest integer? double f = 4.65;int n = (int) f; int n = Math.round(f); int n = (int) Math.round(f);int n = (int) Math.round(100 * f); int n = (int) Math.round(f);Which of the following code fragments converts a floating-point number to the nearest integer? double d = 6.54;int i=(int)Math.round(100 * d); int i=(int)Math.round(d); int i=(int)d; int i=Math.round(d); int i=(int)Math.round(d); Which statement displays the correct average of the value of these variables of type int?println((int1+int2)/2.0); println(int1+int2/2.0); println(int1+int2/2); println((int1+int2)/2); println((int1+int2)/2.0); Which of the following statements is equivalent to balance = balance + amount;?balance += amount; balance == amount; balance +== amount; balance =+ amount; balance += amount; Which of the following statements is equivalent to balance = balance - amount;?balance -= amount; balance =- amount; balance -== amount; balance == amount; balance -= amount; Which of the following statements is equivalent to items = items * 2;?items *= 2; items *== 2; items =* 2; items ==* 2 ; items *= 2; 